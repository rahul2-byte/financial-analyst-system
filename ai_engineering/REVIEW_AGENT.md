# CODE REVIEW AGENT

Purpose:
Review new code for architectural violations.

Checklist:

- Does it violate modularity?
- Does it mix layers?
- Does it allow LLM to compute?
- Does it introduce tight coupling?
- Does it break schema contracts?

Must produce structured review output.
