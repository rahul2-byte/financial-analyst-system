# Benchmark report

## Current result

No market-quality benchmark is currently measured. `evals/gold/v1/` contains a
ten-case synthetic fixture for contract and failure tests. It is not a public
source snapshot and cannot support a market-quality claim.

Required next evidence:

- 100 labeled frozen cases across the five requested groups;
- permitted public source snapshots with retrieval timestamps and SHA-256 hashes;
- two independent human citation and claim labels per scored item, with adjudication for disagreements;
- matching recorded result JSONL;
- controlled Hive usage/latency records and pricing-version metadata;
- separate live runs on three days;
- ablations run against the same gold set.

Do not convert targets into achievements until those artifacts exist.
